services:
  - type: web
    name: comprobante-bot
    env: python
    runtime: python-3.11.9
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHONUNBUFFERED
        value: 1